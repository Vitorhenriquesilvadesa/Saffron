[package]
name = "saffron-http"
version = "0.1.0"
edition = "2024"

[dependencies]
saffron-core = { path = "../saffron-core" }
ureq = { version = "2.10", features = ["json"] }
thiserror = "2.0"
chrono = "0.4"
